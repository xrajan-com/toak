import 'package:flutter/material.dart';

class AppColors {
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const red = Color(0xFFFF2800); // Primary action buttons
  static const blue = Color(0xFF24B6FF); // Secondary actions
}
